// //generate a random number
// //increments by 1
// //square the number
// //print the result on the console

// const generateRandom = () => Math.round(Math.random() * 100);
// const increment = (num: number) => num + 1;
// const square = (num: number) => num * num;
// const printIt = (result: number) => console.log("Result", result);

// function doSomething() {
//     try {
//         let num = generateRandom();
//         let incValue = increment(num);
//         let squaredValue = square(incValue);
//         printIt(squaredValue);
//     } catch (error) {
//         console.log(error);
//     }
// }
// doSomething();
// console.log("end of program");